coverage:
  status:
    project:
      default:
        target: 95%
        threshold: 0%
        informational: false
        if_not_found: failure
    patch:
      default:
        target: 95%
        threshold: 0%
        informational: false
        if_not_found: failure
